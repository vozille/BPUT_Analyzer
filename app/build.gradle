apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'vozille'
            keyPassword 'fakechintu'
            storeFile file('C:/appkeys.jks')
            storePassword 'fakechintu'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId "vozille.myapplication"
        minSdkVersion 17
        targetSdkVersion 23
        versionCode 13
        versionName "1.1.2"
    }
    buildTypes {
        debug {
            debuggable true
            signingConfig signingConfigs.config
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.1.1'
    compile 'com.android.support:design:23.1.1'
    compile 'org.jsoup:jsoup:1.8.3'
    compile files('C:/Users/Admin/AndroidStudioProjects/MyApplication/gson-2.6.1.jar')
    compile files('src/androidTest/java/vozille/myapplication/mpandroidchartlibrary-2-1-6.jar')
}
